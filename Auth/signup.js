/*
const pb = new PocketBase('http://pluggo.cloud.mustini.com');

const allUsers = await pb.collection("users").getFullList(100, { // limit = 100
    sort: "-created"
});

console.log(allUsers); // should be array of objects
*/